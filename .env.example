# Pi-hole YouTube Limiter Configuration
# Copy this file to .env and fill in your values

# Pi-hole host (IP or hostname)
PIHOLE_HOST=192.168.1.x

# SSH user for Pi-hole
PIHOLE_USER=pi

# Path to SSH key (optional, uses default if not set)
# PIHOLE_SSH_KEY=~/.ssh/pihole_key
